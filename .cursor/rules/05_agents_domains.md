---
description: Cursor must use AGENTS.md; act as the agent that owns the path.
globs: "src/**"
alwaysApply: false
---

# Agents and domains

**Cursor must use [AGENTS.md](../../AGENTS.md).** Before editing any file, determine which agent owns it. Act as that agent: follow its Owns, Does not, Depends on, and Collaborates with. Do not change files owned by another agent unless that agent's section allows it.
