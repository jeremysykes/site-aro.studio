# Assets (placeholder)

Planned static assets for Component and ProductCard/ProductSuite. Add real files when ready.

## Logo

- **Header:** `logo.svg` or `logo.png`

## Product icons

One icon or image per ProductSuite item (for ProductCard):

| Product        | Suggested path      |
|----------------|---------------------|
| Token Editor   | `token-editor.svg`  |
| Validator      | `validator.svg`     |
| Figma Sync     | `figma-sync.svg`    |
| Package Builder| `package-builder.svg` |
| Proof Publisher| `proof-publisher.svg` |
| AI Runner      | `ai-runner.svg`     |

Components will reference these via `/assets/<filename>` (e.g. `/assets/logo.svg`, `/assets/token-editor.svg`).
